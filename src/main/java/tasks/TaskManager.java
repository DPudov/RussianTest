package tasks;

/**
 * Created by ${DPudov} on 03.04.2016.
 */
public class TaskManager implements ITaskManager{
    public Task loadFromURL(String url) {
        Task result = new Task();

        return result;
    }

    public Task loadFromMicrosoftAccessDataBase(String directory) {
        Task result = new Task();
        return result;
    }

}
